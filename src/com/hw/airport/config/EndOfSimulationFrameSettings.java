package com.hw.airport.config;

import javax.swing.border.Border;
import java.awt.*;

public class EndOfSimulationFrameSettings extends UserConfigFrameSettings {
    public EndOfSimulationFrameSettings(int mainScrWidth, int mainScrHeight, int exitOp, String mainScrTitle, LayoutManager layoutManager, Color backGroundColor, Color labelColor, Font labelFont, Border borderType) {
        super(mainScrWidth, mainScrHeight, exitOp, mainScrTitle, layoutManager, backGroundColor, labelColor, labelFont, borderType);
    }
}
